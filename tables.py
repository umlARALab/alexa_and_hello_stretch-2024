tables = [(3, 4), (4, 5), (3, 4), (4, 5)]
num_tables = len(tables)

def find_tables():

    # get list of tables from somewhere
    # order closest to farthest
    # assuming vectors as (angle, dist)
    return tables

def set_tables():
    # set tables
    return True

def make_middle_tables():
    del tables[0]
    del tables[(num_tables - 1)]
    # global tables?

def choose_table(num):
    return tables[num]